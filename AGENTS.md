# AGENTS.md

font-tuner のドキュメント（`README.md`・`NOTICE.md`・`docs/*.md`・各 `DESIGN.md`）を書く／直すときのチェック観点。目的は「初見の読者が、何が出荷され、どう動き、なぜそうなっているかを上から順に追えること」。文章・図はコミット前にこの観点で自己点検する。文体は「〜する／〜しない」の規則形に統一し、1行1チェックにする。

機械で拾えるものは [`check-lint.ps1`](check-lint.ps1) にかける（PowerShell は PSScriptAnalyzer、日本語ドキュメントは textlint + prh）。初回だけ `Install-Module PSScriptAnalyzer -Scope CurrentUser` と `npm install` が要る。textlint の対象は、かなの比率で日本語と判定した `.md` だけで、英語で書いた `docs/SPEC.md` 等は対象外。AI っぽい言い回しは [`prh.yml`](prh.yml) の辞書で拾う（指摘が出たら1件足す）。

## 1. 文書の構造
- 上から順に読んで理解できる構成にする（前提を後回しにしない）
- 各節を自己完結させる（前後を行き来しなくても読める）
- 列挙は表で構造化し、散文で長く書かない
- 参照はクリックできるリンクにする（コードスパンや裸のパスで済ませない）
- 整理・言い換えで説明・情報を削らない
- 実装と食い違う記述を残さない（挙動を変えたら同じコミットで docs も直す）

## 2. 前提・事前準備の明示
- ビルドの前提を手順より前に書く（`build-core.ps1` で `build/lib/freetype64.lib` を先に作る、Visual Studio / MSBuild、WiX）
- 任意／必須を正確に区別する
- 反映タイミングを明示する（プロファイル切替・有効/無効・更新は「次に起動するプロセスから」、起動中のプロセスは「プロファイルを再読み込み」）
- 検証手順は、トレイを止めた状態で行うと明記する（動作中のトレイはインストール済みコアを全プロセスに注入するため、2 つのビルドが同じフックを奪い合う）

## 3. 用語・命名
- 製品側の名称は `font-tuner` / `RenderCore64.dll` / `RenderBootstrap64.dll` / `font-tuner.ini` を使う
- 製品のファイル名・設定キー・UI 文言に上流プロジェクト名を使わない。派生元を示す表記としてのみ残す（GPL の帰属に必要なため消さない）
- 略語・専門語は初出で意味を説明する（GDI・DirectWrite・Direct2D・vtable・detour・トランポリン・LCD フィルタ・ローダーロック 等）
- 一般的に使わない造語・独自語を持ち込まない

## 4. 日本語表現
- 自然な技術者の日本語で書く（翻訳調・硬すぎ・砕けすぎを避ける）
- 冗長・重複を除く（同じ語の繰り返し、自明な一文）
- 括弧書きの語を正確にする（例:「1回」→「初回のみ」、「あとで」→「次に起動するプロセスから」）
- 表記を統一する（「全プロセス」「注入」「フック」「アンロード」「常駐固定」）
- 同一文書内で用語を揺らさない（hook／フックを混在させない）
- API 名・ファイル名・パスはコードスパンに入れ、地の文と混ぜない

## 5. 図（構成図の流儀）
- 注入の流れは「誰が・何を・どのプロセスに入れるか」を矢印で示す
- プロセス境界を外枠にし、その中に DLL とフック対象を置く
- 実在する経路だけ描く。届かない経路は図に描かず「届かないもの」の節に文章で書く
- ASCII 図はコードブロックに入れ、折り返さない幅に収める

## 6. 裏取り
- 上流の挙動は `vendor/mactype` の実コードで確認し、推測で書かない
- Windows API の挙動は一次資料（Microsoft Learn）で確認し、似た API からの外挿で断定しない
- 実機で確かめた事実と、未確認の仮説を書き分ける
- 注入 DLL の変更は単一プロセス（`loader` + 対象アプリ）で検証してから書く。「動くはず」を根拠にしない

## 7. この repo 固有の禁止・注意
- 注入 DLL へ上流にない機構（監視スレッド・独自の設定ファイル・ライブ再読込など）を足さない。足すなら結果（スレッドの寿命・アンロード後に何が残るか・そのファイルを誰が書くか・失敗時にどのプロセスが落ちるか）を先に列挙する
- 「全プロセスが同時に落ちる」経路を作らない。作らざるを得ない場合は根拠と検証結果を docs に書く
- `msiexec` は `MSIRESTARTMANAGERCONTROL=Disable` を付けて実行する（Restart Manager がフック DLL を持つ全プロセスを閉じるため）
- ライセンスは GPL-3.0-only と書く（`-or-later` にしない。上流が「or later」を許諾していない）

## 8. コミュニケーション姿勢
- 結論を先に一文で書く
- 聞かずにやり、メニューや「区切りますか」を出さない
- 謝罪・自己分析を繰り返さない（誤りは行動で直す）
- コスト/トークンを判断材料にしない
- メールアドレスを出さない（GitHub noreply を使う）／コミット・PR にセッション URL を入れない
